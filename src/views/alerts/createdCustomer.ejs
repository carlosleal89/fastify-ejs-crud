<% if (criado) { %>
  <script>
    // Exibe o alerta
    window.onload = function() {
      alert("Cliente criado com sucesso!");
      // Redireciona após o usuário clicar em OK
      window.location.href = "/customers/"; // Redireciona para a tela principal
    };
  </script>
<% } %>